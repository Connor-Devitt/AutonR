package recording;

import java.io.*;
import java.util.Scanner;

public class WriterDriver {
  public static void main(String args[]) {
    //I dont know what createActionMethod does
    
    Scanner scan = new Scanner(System.in);
    File outputFile;
    String path;
    
    System.out.println("Type the path to the designated output File.");
    path = scan.nextLine();
    
    BufferedFileWriter outputStream = new BufferedFileWriter(new fileWriter(new File(path));
    
    outputStream.write(Writer.write(Writer.recieveAction());
  }
}
